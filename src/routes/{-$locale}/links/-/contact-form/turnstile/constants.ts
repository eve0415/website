/** Public widget sitekey. Safe to ship to the browser. */
export const TURNSTILE_SITE_KEY = '0x4AAAAAAEQg5fG4R1lw_GQr';

/** Rendered with the widget and re-checked on the server, so the two must agree. */
export const TURNSTILE_ACTION = 'contact';
